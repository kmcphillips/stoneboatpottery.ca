%div.post
  %h2.title Edit Link

  %div.entry
    - form_for [:admin, @link] do |f| 
      = render :partial => 'admin/links/form', :locals => {:f => f}
      = f.submit "Save"

  %div.actions
    - index_entity_image(admin_links_path)
    = link_to "Back to all links", admin_links_path
