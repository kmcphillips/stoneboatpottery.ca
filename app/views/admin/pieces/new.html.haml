%div.post
  %h2.title New Piece

  %div.entry
    - form_for [:admin, @piece], :method => :post do |f| 
      = render :partial => 'admin/pieces/form', :locals => {:f => f}
      = f.submit "Create New Piece"

  %div.actions
    = index_entity_image(admin_pieces_path, "Back to all pieces")

